import 'package:flutter/material.dart';

import '../widgets/widgets_barrel.dart';

class HomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: LogBackground());
  }
}
